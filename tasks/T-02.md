        ---
        repo: cstack-abtest-work
        domain: be
        done_check: python3 -m unittest tests.test_02_filter_by_tag
        e2e_check: '-'
        lease_hours: 2
        ready: true
        ---
        # T-02 — Implement filter_by_tag(items, tag)

        ## Traceability
        - Requirement: ad hoc — A/B test task
        - Repo: cstack-abtest-work
        - Domain: be

        ## Context
        `filter_by_tag(items, tag)` filters a list of content dicts by a tag. It is currently stubbed in `content_service/features.py` and raises
        `NotImplementedError`. Implement it — a pure function, no I/O or shared state.

        ## Acceptance criteria
        - AC1: returns only items whose 'tags' list contains `tag`
- AC2: returns an empty list when no item has the tag
- AC3: input order is preserved

        ## AC → verification mapping
        | AC | Verified by | Type |
        |---|---|---|
        | AC1 | `python3 -m unittest tests.test_02_filter_by_tag` | done_check |
| AC2 | `python3 -m unittest tests.test_02_filter_by_tag` | done_check |
| AC3 | `python3 -m unittest tests.test_02_filter_by_tag` | done_check |

        ## Verification
        The gate is exactly: `python3 -m unittest tests.test_02_filter_by_tag` — it must exit 0. Do not edit the test.

        ## Out of scope
        - Any other function in `content_service/features.py` (each has its own task).
        - Adding dependencies — stdlib only.

        ## Definition of done
        `python3 -m unittest tests.test_02_filter_by_tag` passes and no other test regresses.
