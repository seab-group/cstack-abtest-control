        ---
        repo: cstack-abtest-work
        domain: be
        done_check: python3 -m unittest tests.test_01_paginate
        e2e_check: '-'
        lease_hours: 2
        ready: true
        ---
        # T-01 — Implement paginate(items, page, size)

        ## Traceability
        - Requirement: ad hoc — A/B test task
        - Repo: cstack-abtest-work
        - Domain: be

        ## Context
        `paginate(items, page, size)` returns a page of a list. It is currently stubbed in `content_service/features.py` and raises
        `NotImplementedError`. Implement it — a pure function, no I/O or shared state.

        ## Acceptance criteria
        - AC1: paginate(items, page, size) returns the page-th slice (1-indexed) of `size` items
- AC2: size is clamped to the range 1..100 (values below 1 become 1, above 100 become 100)
- AC3: a page past the end of the list returns an empty list

        ## AC → verification mapping
        | AC | Verified by | Type |
        |---|---|---|
        | AC1 | `python3 -m unittest tests.test_01_paginate` | done_check |
| AC2 | `python3 -m unittest tests.test_01_paginate` | done_check |
| AC3 | `python3 -m unittest tests.test_01_paginate` | done_check |

        ## Verification
        The gate is exactly: `python3 -m unittest tests.test_01_paginate` — it must exit 0. Do not edit the test.

        ## Out of scope
        - Any other function in `content_service/features.py` (each has its own task).
        - Adding dependencies — stdlib only.

        ## Definition of done
        `python3 -m unittest tests.test_01_paginate` passes and no other test regresses.
