        ---
        repo: cstack-abtest-work
        domain: be
        done_check: python3 -m unittest tests.test_10_bulk_add
        e2e_check: '-'
        lease_hours: 2
        ready: true
        ---
        # T-10 — Implement bulk_add(items, new_items)

        ## Traceability
        - Requirement: ad hoc — A/B test task
        - Repo: cstack-abtest-work
        - Domain: be

        ## Context
        `bulk_add(items, new_items)` inserts many items atomically. It is currently stubbed in `content_service/features.py` and raises
        `NotImplementedError`. Implement it — a pure function, no I/O or shared state.

        ## Acceptance criteria
        - AC1: each new item must have a non-empty 'title'; otherwise raise ValueError and add none (atomic)
- AC2: on success returns items + new_items, each new item given a unique integer 'id'
- AC3: does not mutate the input list

        ## AC → verification mapping
        | AC | Verified by | Type |
        |---|---|---|
        | AC1 | `python3 -m unittest tests.test_10_bulk_add` | done_check |
| AC2 | `python3 -m unittest tests.test_10_bulk_add` | done_check |
| AC3 | `python3 -m unittest tests.test_10_bulk_add` | done_check |

        ## Verification
        The gate is exactly: `python3 -m unittest tests.test_10_bulk_add` — it must exit 0. Do not edit the test.

        ## Out of scope
        - Any other function in `content_service/features.py` (each has its own task).
        - Adding dependencies — stdlib only.

        ## Definition of done
        `python3 -m unittest tests.test_10_bulk_add` passes and no other test regresses.
