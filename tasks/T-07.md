---
repo: cstack-abtest-work
domain: be
done_check: python3 -m unittest tests.test_07_validate_title
e2e_check: '-'
lease_hours: 2
ready: true
---
# T-07 — Implement validate_title(title)

## Traceability
- Requirement: ad hoc — A/B test task
- Repo: cstack-abtest-work
- Domain: be

## Context
`validate_title(title)` validates and normalises a content title. It is currently stubbed in `content_service/features.py` and raises
`NotImplementedError`. Implement it — a pure function, no I/O or shared state.

## Acceptance criteria
- AC1: returns the title stripped of leading/trailing whitespace
- AC2: raises ValueError when the title is empty or only whitespace

## AC → verification mapping
| AC | Verified by | Type |
|---|---|---|
| AC1 | `python3 -m unittest tests.test_07_validate_title` | done_check |
| AC2 | `python3 -m unittest tests.test_07_validate_title` | done_check |

## Verification
The gate is exactly: `python3 -m unittest tests.test_07_validate_title` — it must exit 0. Do not edit the test.

## Out of scope
- Any other function in `content_service/features.py` (each has its own task).
- Adding dependencies — stdlib only.

## Definition of done
`python3 -m unittest tests.test_07_validate_title` passes and no other test regresses.
