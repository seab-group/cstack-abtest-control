---
repo: cstack-abtest-work
domain: be
done_check: python3 -m unittest tests.test_06_word_count
e2e_check: '-'
lease_hours: 2
ready: true
---
# T-06 — Implement word_count(text)

## Traceability
- Requirement: ad hoc — A/B test task
- Repo: cstack-abtest-work
- Domain: be

## Context
`word_count(text)` counts words in a string. It is currently stubbed in `content_service/features.py` and raises
`NotImplementedError`. Implement it — a pure function, no I/O or shared state.

## Acceptance criteria
- AC1: returns the number of whitespace-separated words
- AC2: collapses runs of whitespace (does not count empty tokens)
- AC3: returns 0 for an empty or whitespace-only string

## AC → verification mapping
| AC | Verified by | Type |
|---|---|---|
| AC1 | `python3 -m unittest tests.test_06_word_count` | done_check |
| AC2 | `python3 -m unittest tests.test_06_word_count` | done_check |
| AC3 | `python3 -m unittest tests.test_06_word_count` | done_check |

## Verification
The gate is exactly: `python3 -m unittest tests.test_06_word_count` — it must exit 0. Do not edit the test.

## Out of scope
- Any other function in `content_service/features.py` (each has its own task).
- Adding dependencies — stdlib only.

## Definition of done
`python3 -m unittest tests.test_06_word_count` passes and no other test regresses.
