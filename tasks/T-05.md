---
repo: cstack-abtest-work
domain: be
done_check: python3 -m unittest tests.test_05_slugify
e2e_check: '-'
lease_hours: 2
ready: true
---
# T-05 — Implement slugify(title)

## Traceability
- Requirement: ad hoc — A/B test task
- Repo: cstack-abtest-work
- Domain: be

## Context
`slugify(title)` turns a title into a URL slug. It is currently stubbed in `content_service/features.py` and raises
`NotImplementedError`. Implement it — a pure function, no I/O or shared state.

## Acceptance criteria
- AC1: lowercases the title
- AC2: replaces each run of non-alphanumeric characters with a single '-'
- AC3: strips leading and trailing '-'

## AC → verification mapping
| AC | Verified by | Type |
|---|---|---|
| AC1 | `python3 -m unittest tests.test_05_slugify` | done_check |
| AC2 | `python3 -m unittest tests.test_05_slugify` | done_check |
| AC3 | `python3 -m unittest tests.test_05_slugify` | done_check |

## Verification
The gate is exactly: `python3 -m unittest tests.test_05_slugify` — it must exit 0. Do not edit the test.

## Out of scope
- Any other function in `content_service/features.py` (each has its own task).
- Adding dependencies — stdlib only.

## Definition of done
`python3 -m unittest tests.test_05_slugify` passes and no other test regresses.
