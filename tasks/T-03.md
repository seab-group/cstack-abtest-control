---
repo: cstack-abtest-work
domain: be
done_check: python3 -m unittest tests.test_03_search
e2e_check: '-'
lease_hours: 2
ready: true
---
# T-03 — Implement search(items, query)

## Traceability
- Requirement: ad hoc — A/B test task
- Repo: cstack-abtest-work
- Domain: be

## Context
`search(items, query)` does a case-insensitive substring search over title and body. It is currently stubbed in `content_service/features.py` and raises
`NotImplementedError`. Implement it — a pure function, no I/O or shared state.

## Acceptance criteria
- AC1: matches when `query` is a case-insensitive substring of the item's title OR body
- AC2: returns an empty list when nothing matches
- AC3: an empty query matches everything

## AC → verification mapping
| AC | Verified by | Type |
|---|---|---|
| AC1 | `python3 -m unittest tests.test_03_search` | done_check |
| AC2 | `python3 -m unittest tests.test_03_search` | done_check |
| AC3 | `python3 -m unittest tests.test_03_search` | done_check |

## Verification
The gate is exactly: `python3 -m unittest tests.test_03_search` — it must exit 0. Do not edit the test.

## Out of scope
- Any other function in `content_service/features.py` (each has its own task).
- Adding dependencies — stdlib only.

## Definition of done
`python3 -m unittest tests.test_03_search` passes and no other test regresses.
