---
repo: cstack-abtest-work
domain: be
done_check: python3 -m unittest tests.test_04_sort_items
e2e_check: '-'
lease_hours: 2
ready: true
---
# T-04 — Implement sort_items(items, key, desc=False)

## Traceability
- Requirement: ad hoc — A/B test task
- Repo: cstack-abtest-work
- Domain: be

## Context
`sort_items(items, key, desc=False)` sorts a list of dicts by a key. It is currently stubbed in `content_service/features.py` and raises
`NotImplementedError`. Implement it — a pure function, no I/O or shared state.

## Acceptance criteria
- AC1: returns a new list sorted ascending by `items[i][key]`
- AC2: `desc=True` sorts descending
- AC3: the input list is not mutated

## AC → verification mapping
| AC | Verified by | Type |
|---|---|---|
| AC1 | `python3 -m unittest tests.test_04_sort_items` | done_check |
| AC2 | `python3 -m unittest tests.test_04_sort_items` | done_check |
| AC3 | `python3 -m unittest tests.test_04_sort_items` | done_check |

## Verification
The gate is exactly: `python3 -m unittest tests.test_04_sort_items` — it must exit 0. Do not edit the test.

## Out of scope
- Any other function in `content_service/features.py` (each has its own task).
- Adding dependencies — stdlib only.

## Definition of done
`python3 -m unittest tests.test_04_sort_items` passes and no other test regresses.
