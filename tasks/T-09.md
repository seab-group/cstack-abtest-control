---
repo: cstack-abtest-work
domain: be
done_check: python3 -m unittest tests.test_09_update_item
e2e_check: '-'
lease_hours: 2
ready: true
---
# T-09 — Implement update_item(items, item_id, changes)

## Traceability
- Requirement: ad hoc — A/B test task
- Repo: cstack-abtest-work
- Domain: be

## Context
`update_item(items, item_id, changes)` applies a partial update to one item. It is currently stubbed in `content_service/features.py` and raises
`NotImplementedError`. Implement it — a pure function, no I/O or shared state.

## Acceptance criteria
- AC1: returns a new list where the matching item has `changes` merged in (other fields kept)
- AC2: does not mutate the input list
- AC3: raises KeyError when no item has that id

## AC → verification mapping
| AC | Verified by | Type |
|---|---|---|
| AC1 | `python3 -m unittest tests.test_09_update_item` | done_check |
| AC2 | `python3 -m unittest tests.test_09_update_item` | done_check |
| AC3 | `python3 -m unittest tests.test_09_update_item` | done_check |

## Verification
The gate is exactly: `python3 -m unittest tests.test_09_update_item` — it must exit 0. Do not edit the test.

## Out of scope
- Any other function in `content_service/features.py` (each has its own task).
- Adding dependencies — stdlib only.

## Definition of done
`python3 -m unittest tests.test_09_update_item` passes and no other test regresses.
